#!python3 
# -*- coding: utf-8 -*-

from managers.filemanager import FileManager
from managers.cliexecutor import CmdLineExecutor
from domain.fileentry import JavaFileEntry 

class ProjectsMerger:
	def __init__(self,target_project)
	cli_executor = CmdLineExecutor()
	file_manager = FileManager()

	def merge_javaentry(java_entry,target_project):
		imports = javaentry.child_imports
		for imported_class in child_imports:
			if imported_class.status = "NEW" 
				self.merge_new_javaentry(imported_class)			
			if imported_class.status = "MODIFIED" 
				self.merge_modified_java_entry(imported_class):

	def merge_new_file(java_entry):
		pass

	def merge_modified_java_entry(java_entry):
		merge_command = get_
		cli_executor(,"kdiff3")


	def get_merge_cmd_line(java_entry):
		pass
